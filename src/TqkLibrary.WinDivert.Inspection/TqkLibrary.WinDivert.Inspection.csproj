<Project Sdk="Microsoft.NET.Sdk">

	<Import Project="..\ProjectBuildProperties.targets" />

	<PropertyGroup>
		<RootNamespace>TqkLibrary.WinDivert.Inspection</RootNamespace>
		<AssemblyName>TqkLibrary.WinDivert.Inspection</AssemblyName>
		<Description>Reads the host name a TCP client asks for (TLS SNI, HTTP Host) without consuming the stream. No WinDivert dependency.</Description>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
	</ItemGroup>

</Project>
